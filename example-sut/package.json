{
  "name": "phrase-example-sut",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "cd packages/client && npm run build && cd ../server && node server.js",
    "dev": "concurrently \"cd packages/client && npm start\" \"cd packages/server && node server.js\"",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cypress": "^13.5.0"
  }
}
